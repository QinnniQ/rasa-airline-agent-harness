[project]
name = "rasa-airline-agent-harness"
version = "0.1.0"
description = "A beginner-friendly Rasa MCP agent harness for an airline customer service assistant."
requires-python = ">=3.11,<3.12"
dependencies = [
    "rasa-pro>=3.16",
]

[tool.uv]
package = false